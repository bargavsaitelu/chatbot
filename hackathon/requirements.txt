# Used by pip to install required python packages
# Usage: pip install -r requirements.txt
# If getting errors about puntk then go to python console and type
# >>> import nltk
# >>> nltk.download()
# Then an installation window appears. Go to 'Models' tab and select 'puntk' under 'identifier' column. Then click Download.
nltk==3.4.5
tensorflow==2.7.2
tflearn==0.3.2
flask>=1.1.0
